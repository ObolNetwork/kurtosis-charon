participants:
  - el_type: geth
    el_image: ethereum/client-go:${EL_VERSION}
    cl_type: lodestar
    cl_image: chainsafe/lodestar:${CL_VERSION}
    use_separate_vc: true
    count: 3
    prometheus_config:
      scrape_interval: 15s
      labels: {}
