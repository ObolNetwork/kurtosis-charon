participants:
  - el_type: geth
    el_image: ${EL_IMAGE}:${EL_VERSION}
    cl_type: lodestar
    cl_image: ${CL_IMAGE}:${CL_VERSION}
    supernode: true
    use_separate_vc: true
    count: 3
    prometheus_config:
      scrape_interval: 15s
      labels: {}
